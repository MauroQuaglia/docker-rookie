# Miscellanea
* Le iptables sono come un firewall locale integrato in Linux e decidono quale traffico può entrare o uscire.
    * Rete -> Kernel Linux -> iptables -> Applicazione
    * `sudo iptables -L -n -v`
* Anche queste sono utili: `sudo iptables -t nat -L -n -v`
