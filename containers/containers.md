* Nella risoluzione nomi-di-dominio a IP nel container
* `/etc/hosts` (cerca cose che gli ho dato io in locale)
* `/etc/resolv.conf` che è basato su quello del docker host `/run/systemd/resolve/resolv.conf`